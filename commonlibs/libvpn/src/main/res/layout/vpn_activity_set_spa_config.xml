<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="5dp"
    tools:context=".view.SetSpaConfigActivity">

    <EditText
        android:id="@+id/spa_config_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:minLines="10"
        android:maxLines="10"
        android:gravity="top"
        android:text=""
        android:hint="请输入spa配置"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:autofillHints="" />

    <Button
        android:id="@+id/set_spa_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/spa_config_text"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:background="@drawable/auth_button_login"
        android:text="保存Spa配置"
        android:textColor="#ffffff"
        android:layout_marginTop="10dp"
        android:textSize="20sp" />


</androidx.constraintlayout.widget.ConstraintLayout>